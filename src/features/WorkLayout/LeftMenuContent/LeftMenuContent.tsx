import classNames from 'classnames';
import styles from './LeftMenuContent.module.scss';

export function LeftMenuContent() {
  return (
    <nav className={classNames(styles.LeftMenuContent)}>LeftMenuContent</nav>
  );
}
